{
  "_comment": "Use the root vercel.json at the repository root. Set Vercel Root Directory to '.' (not frontend) so /api deploys.",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite"
}
